# LLMem Ecosystem — OpenCode Integration

> **This content has moved.** See [Integrations](INTEGRATIONS.md) for the complete integration guide covering OpenCode, Copilot CLI, and custom tools.

Quick install:

```bash
curl -sSL https://raw.githubusercontent.com/MichielDean/LLMem/main/setup.sh | bash
```

Manual install from source:

```bash
git clone https://github.com/MichielDean/LLMem.git
cd LLMem && ./setup.sh
```

For provider configuration, see [Providers](PROVIDERS.md).