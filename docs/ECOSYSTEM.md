# LLMem Ecosystem — Agent Integration

> **This content has moved.** See [Integrations](INTEGRATIONS.md) for the complete integration guide covering OpenCode, Claude Code, Copilot CLI, and custom tools.

Quick install:

```bash
git clone https://github.com/MichielDean/LLMem.git
cd LLMem && make build && make install
npm install   # deploys skills + plugins for your platform
llmem init
```

For provider configuration, see [Providers](PROVIDERS.md).