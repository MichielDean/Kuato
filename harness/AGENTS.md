# AGENTS.md — Decomposed

This file has been decomposed into three separate instruction files:

- **`identity.md`** — Agent personality, soul, and boundaries
- **`user.md`** — About the user
- **`rules.md`** — Generic workflow rules

All three are loaded via the `instructions` field in `opencode.json`. This file is no longer the source of truth.